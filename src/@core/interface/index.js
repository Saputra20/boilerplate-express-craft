const IPayload = require('./payload.interface');
const IPagination = require('./pagination.interface');
const IToken = require('./token.interface');
const IGuard = require('./guard.interface');

module.exports = {
  IPayload,
  IPagination,
  IToken,
  IGuard,
};
